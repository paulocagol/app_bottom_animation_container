name: cart_animation_component
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  device_preview: ^1.2.0
  flutter:
    sdk: flutter
  local_hero: ^0.3.0
  smooth_sheets: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img.png
